import { TbFidgetSpinner } from "react-icons/tb";
import { categories } from "../Categories/categoriesData";

const CreateListsForm = ({
  handleSubmit,
  loading,
  handleImageChange,
  uploadButtonText,
  handleImageChange2,
  uploadButtonText2,
}) => {
  return (
   
  );
};

export default CreateListsForm;
